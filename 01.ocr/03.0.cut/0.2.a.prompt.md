now 21 line is too far right. For such cases manual correction for start point is needed afaik.

so please:





patch the notebook loop place so that it accepts and applies if provided, the corrections per page list. and also export current all starting points (found or specified) to a unified json, And another json for each image overlay red line drawing, just as you did in Sample overlay* files you presented



create python file that is a tool:





allowing to select folder



starts showing image in that folder, with left/right buttons (navigation between images)



shows overlay line, and allows it to be moved with numeric texbox accepting positive/negative pixels



shows mouse coords in status bar alike area in two forms: 1) picture x, y (just like in gimp/paind) 2) x coord +- from the overlay coord for current file. The numbox saves result in memory copy of the corrections json and when save all button is pressed, then it is exported overwriting the corrections json file. Also i think it is good idea to copy the line seek function, and draw new overlay line with the same algo as in notebook and update the overlays file too on save all overlays press.



if there are unviewed images, do not fill their starting point nor overlay data.



also as the data is two way, please add json markers at notebook as autogen: True, but if modified and saved from gui tool, then autogen: False



finally, notebook, if not only starting points json is provided, but also overlay lines file, then for images that have corresponding overlay line data, take that precomputed one. if not, but there is start point data, take into account start point data. If autogen overlay, but conflicting start point, manual has the preference.

