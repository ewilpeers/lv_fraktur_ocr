Actually, simpler: replace the dual's per-row decision with a decision tree using recent history instead of just "is run_pick within snap_thresh of seed_x". I'll add a 1-track running history to the dual algorithm. If recent-N picks have shown high jitter (entering letters), prefer vertical pick more aggressively. If recent-N picks have been stable (genuine curve), trust them.
Hmm, let me step back. Both my dual and multitrack are at ~80% as good as ideal. For the user's actual workflow, which is "auto-detect mostly works, GUI fixes the bad cases", a small fraction of slightly-suboptimal pages is fine. The user can fix gold_00021 and gold_00013 with a click in the GUI now that zoom works.