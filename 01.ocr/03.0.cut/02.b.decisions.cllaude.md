A couple of things I need to decide before coding:
What does the +/- numeric box actually shift? Two reasonable interpretations: (a) the seed x (single number; we re-trace from there), or (b) every x in the curve (rigid horizontal shift of the whole overlay). I'll go with (a) shift the seed and re-trace — that's what your message implies ("copy the line seek function, and draw new overlay line with the same algo"), and it gives the best result on pages where the auto-seed missed but the line itself is real.

Should the GUI also let the user click on the image to set the seed directly? That's much faster than typing pixel offsets when the seed is way off. I'll add it as a bonus (click sets a new absolute seed x), keeping the numeric box for fine adjustment.

The notebook's batch cell needs to know the JSON paths. I'll add config keys STARTS_JSON and OVERLAYS_JSON defaulting to files next to TARGET_DIR.

Refactor: I'll extract the detection logic into a small detection.py module that both the notebook and the GUI import. This avoids duplicating the algorithm and guarantees they stay in sync.